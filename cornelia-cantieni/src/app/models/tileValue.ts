export class TileValue {
    title: string;
    text: string;
    btnText: string;

    constructor(
        title: string,
        text: string,
        btnText: string
    ) {
        this.title = title;
        this.text = text;
        this.btnText = btnText
    }
}
